@model List<DeckArchief.ViewModels.ViewDecks>

@{
    ViewData["Title"] = "Decks";
}
<h2>@ViewData["Title"]</h2>
<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="Home">Home</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01" data-np-autofill-form-type="other" data-np-checked="1" data-np-watching="1">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="Brands">
						Brands
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Collections</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="Decks">Decks</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Producers</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Wishlists</a>
				</li>
			</ul>
			<form class="d-flex">
				<input class="form-control me-sm-2" type="search" placeholder="Search" data-np-intersection-state="visible">
				<button class="btn btn-info my-2 my-sm-0" type="submit">Search</button>
			</form>
		</div>
	</div>
</nav>
<hr />
<form>
	<button asp-action="Create" asp-controller="Decks" class="btn btn-primary mb-3">Add new deck</button>
	<table class="table table-responsive table-bordered table-primary">
		<thead>
			<tr class="table-active">
				<th>Id</th>
				<th>Name</th>
				<th>Series</th>
				<th>Price</th>
				<th>Brand</th>
				<th>Producer</th>
				<th>Designer</th>
				<th>Color</th>
				<th>Current value</th>
			</tr>
		</thead>
		<tbody>
			@if (Model != null && Model.Any())
			{
				@foreach (var deck in Model)
				{
					<tr>
						<td>@deck.Id</td>
						<td>@deck.Name</td>
						<td>@deck.Series</td>
						<td>@deck.Price</td>
						<td>@deck.BrandName</td>
						<td>@deck.ProducerName</td>
						<td>@deck.Designer</td>
						<td>@deck.Color</td>
						<td>@deck.CurrentValue1</td>
						<td>
							<div class="btn-group btn-group-sm">
								<a asp-controller="Decks" asp-action="Edit" asp-route-id="@deck.Id" class="btn btn-light">Edit</a>
								<a asp-controller="Decks" asp-action="Delete" asp-route-id="@deck.Id" class="btn btn-danger">Delete</a>
							</div>
						</td>
					</tr>
				}
			}
			else
			{
				<tr>
					<td colspan="9">
						<div>
							At this point in time, there are no decks available. Please try again later.
						</div>
					</td>
				</tr>
			}
		</tbody>
	</table>
</form>

